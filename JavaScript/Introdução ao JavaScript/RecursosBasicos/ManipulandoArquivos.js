//Comentario de linha

/*Comentario
varias
linhas */

//Variaveis var: Os valores podem mudar no codigo, variavel dinamica
//Constantes Const: valor não pode ser mudado, "variavel" estatica

var preco = 2;
var desc = 0.2;

const PRECO2=2
var total = PRECO2 - desconto;

preco = preco - desc;

total = PRECO2 - desc;